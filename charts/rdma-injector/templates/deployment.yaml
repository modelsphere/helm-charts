apiVersion: apps/v1
kind: Deployment
metadata:
  name: rdma-injector
  namespace: {{ .Release.Namespace }}
  labels: { app: rdma-injector }
spec:
  replicas: {{ .Values.replicas }}
  selector: { matchLabels: { app: rdma-injector } }
  template:
    metadata: { labels: { app: rdma-injector } }
    spec:
      tolerations:                                  # allow scheduling onto control-plane nodes
      - { operator: Exists }
      affinity:                                     # prefer to spread replicas across nodes
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector: { matchLabels: { app: rdma-injector } }
              topologyKey: kubernetes.io/hostname
      containers:
      - name: webhook
        # The code is in the image; the serving certificate is self-signed by
        # Helm into a Secret and mounted at /tls.
        image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports: [{ containerPort: 8443 }]
        readinessProbe:
          httpGet: { path: /healthz, port: 8443, scheme: HTTPS }
          initialDelaySeconds: 2
        resources: {{ toYaml .Values.resources | nindent 10 }}
        volumeMounts:
        - { name: tls, mountPath: /tls, readOnly: true }
      volumes:
      - name: tls
        secret: { secretName: rdma-injector-certs }
