apiVersion: v2
name: bodylog-exporter
description: "Turns the body-log listener's detail records into Prometheus metrics, and polls the router's control-plane endpoints for live routing state"
home: https://github.com/modelsphere/helm-charts
sources:
  - https://github.com/modelsphere/helm-charts
  - https://github.com/modelsphere/llm-openresty
type: application
# The chart version tracks appVersion for this chart -- the same numbering the
# internal GitLab repo publishes -- so one pinned version resolves against
# either repository. Not a rule for every chart here: cart carries a chart
# version of its own, independent of the app it ships.
version: 0.2.6
# The exporter image tag: values.image.tag is empty by default and falls back to
# this, so a release moves both together.
#
# 0.2.6 is the actual release line -- it is the newest exporter/v* tag upstream,
# the version ChartMuseum serves, and the image the clusters run. "1.0.0" was a
# placeholder that an internal pipeline rewrote from the tag at publish time, so
# it named a release that never existed, and no such image was ever built.
appVersion: "0.2.6"
keywords: [bodylog, exporter, prometheus, observability]
